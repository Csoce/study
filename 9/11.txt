11：
	1：vector<int> a;
	2: vector<int> b{1,2,3,4};
	3: vector<int> c(10);
	4: vector<int> d(10,1);
	5: vector<int> e(b.begin(),b,end());
	6: vector<int> f(b);

12:	
	接受一个容器创建其拷贝构造的函数必须类型相同 
	接受迭代器的只需要元素类型相容就行,且容器类型可以不同


